import React from 'react'
import ReactDOM from 'react-dom/client'
import EnglishSurvivalApp from './EnglishSurvivalApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EnglishSurvivalApp />
  </React.StrictMode>,
)
